- hosts: all
  become: True
  gather_facts: True
  tasks:

    - name: install docker etc
      import_role:
        name: install

    - name: run the nginx base demo
      import_role:
        name: run
