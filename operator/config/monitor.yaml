apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: monitors.google.dev
  labels:
    type: monitor
spec:
  scope: Namespaced
  group: google.dev
  names:
    kind: Monitor
    plural: monitors
    singular: monitor
    shortNames:
      - monitor
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                nodes:
                  description: |
                    A list of node exporter endpoints.
                  type: array
                  items:
                    type: string
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true