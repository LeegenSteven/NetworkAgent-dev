- hosts: localhost
  gather_facts: false
  tasks:
    - name: Get a ComputeAddress object
      kubernetes.core.k8s_info:
        api_version: compute.cnrm.cloud.google.com/v1beta1
        kind: ComputeAddress
        name: '{{ edge_ip_name }}'
        namespace: automation
