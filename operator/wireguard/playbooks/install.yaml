- hosts: all
  become: yes
  gather_facts: no
  tasks:

  - name: run install
    import_role:
      name: install

  - name: run wireguard
    become: yes
    ansible.builtin.systemd_service:
      state: restarted
      enabled: true
      name: wg-quick@wg0
