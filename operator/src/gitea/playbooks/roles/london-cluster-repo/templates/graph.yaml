---
apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: "default"
  name: networkoperator-account

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: networkoperator-rolebinding-cluster
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
  - kind: ServiceAccount
    name: networkoperator-account
    namespace: "default"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: graph-operator
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      application: graph-operator
  template:
    metadata:
      labels:
        application: graph-operator
    spec:
      serviceAccountName: networkoperator-account
      containers:
      - name: graph-operator
        image: {{GOOGLE_REGION}}-docker.pkg.dev/{{GOOGLE_PROJECT}}/netorkagent/networkoperator:latest
        imagePullPolicy: Always
        env:
        - name: BASEDIR
          value: "/operator"
        - name: GOOGLE_PROJECT
          value: {{GOOGLE_PROJECT}}
        - name: GOOGLE_REGION
          value: {{GOOGLE_REGION}}
        - name: GOOGLE_ZONE
          value: {{GOOGLE_ZONE}}
        - name: GOOGLE_USER
          value: admin_briannaughton_altostrat_co
        - name: GRAPH
          value: "yes"
