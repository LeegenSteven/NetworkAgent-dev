apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: userplanefunctions.google.dev
  # labels:
  #   type: connectivityservice
spec:
  scope: Namespaced
  group: google.dev
  names:
    kind: UserPlaneFunction
    plural: userplanefunctions
    singular: userplanefunction
    shortNames:
      - upf
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                interface: 
                  description: The network interface
                  type: object
                  properties:
                    name:
                      description: name of the vpc
                      type: string
                    namespace:
                      description: name of the vpc namespace
                      type: string
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true